namespace IdleOn.Items
{
    public enum ItemType       { Equipment, Consumable, Material }
    public enum EquipmentSlot  { Hat, Weapon, Armor, Accessory, Pants, Shoes, Ring1, Ring2 }
    public enum ConsumableType { HealthPotion, ManaPotion, InventoryExpansion }
    public enum ItemRarity     { Common, Uncommon, Rare, Epic }
    public enum CurrencyType   { Silver, Gold }
    public enum DropType       { Item, Currency }
}
